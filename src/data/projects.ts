/** Portfolio window layer — re-exports OS grid project rows (architecture §5). */
export type { OsProject as PortfolioProject } from "@/app/data/projectsData";
export { osProjects as projects } from "@/app/data/projectsData";
